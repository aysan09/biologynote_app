<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biologyapp">

    <application

        android:allowBackup="true"
        android:label="Biology Reference App"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_Light"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">


        <!-- Launcher activity: must be exported -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities: not exported because they are internal -->
        <activity
            android:name=".TopicListActivity"
            android:exported="false" />
        <activity
            android:name=".DetailActivity"
            android:exported="false" />

    </application>
</manifest>
